```
@startuml
title Варианты использования программного обеспечения банкомата

|Пользователь|
start

:Вставить карту;
if (Карта корректная) then (да)
  :Ввести ПИН-код;
  if (ПИН-код верный) then (да)
    :Выбрать тип операции;
    if (Снять наличные) then (да)
      :Ввести сумму;
      if (Достаточно средств) then (да)
        :Выдать наличные;
      else (нет)
        :Предложить ввести меньшую сумму;
      endif
    elseif (Пополнить счет) then (да)
      :Ввести сумму;
      :Внести деньги;
    elseif (Проверить баланс) then (да)
      :Получить информацию о балансе;
    endif
  else (нет)
    :Предложить ввести ПИН-код еще раз;
  endif
else (нет)
  :Изъять карту;
endif

stop
@enduml
```
